# 旧版实现归档

本目录保存早期综合评价版本的兼容代码和旧图表，仅用于追溯，不参与当前 2.0 主流程。

- `utils.py`：旧版评价模型工具代码。
- `visualize.py`：旧版图表生成代码。
- `charts/`：仅由旧版可视化代码生成、未被当前论文和网页引用的图表。

正式运行入口为 `信贷决策建模/main.py`，正式图表位于 `信贷决策建模/output/charts/`。
